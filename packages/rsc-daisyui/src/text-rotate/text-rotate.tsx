import { classed } from "../classed.config";
import { configWithThemeFn } from "../config";

export const TextRotate = classed("span", "text-rotate", configWithThemeFn({}));
TextRotate.displayName = "TextRotate";
